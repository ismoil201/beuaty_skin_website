# Production API (Vite exposes VITE_* to the client bundle)
VITE_API_BASE_URL=https://cosmetic-server-production.up.railway.app

# Optional: local static server port when using `npm run start`
PORT=4174
