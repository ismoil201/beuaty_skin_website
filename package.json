{
  "name": "beauty-skin-korea",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run sync:css && vite",
    "build": "npm run sync:css && vite build",
    "preview": "vite preview",
    "start": "node server.cjs",
    "sync:css": "node scripts/sync-css.cjs",
    "test": "node --test tests/authHttp.test.js tests/pendingActionManager.test.js",
    "web:dev": "npm --prefix web run dev",
    "web:build": "npm --prefix web run build",
    "web:preview": "npm --prefix web run preview"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  },
  "dependencies": {
    "firebase": "^12.15.0"
  }
}
